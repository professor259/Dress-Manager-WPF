<UserControl x:Class="Attendance.View.AddDress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Attendance.View"
              xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"  
             mc:Ignorable="d" 
             Background="Transparent"             
           
                 Style="{StaticResource Page_Style}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBoxBase}">
                        <Border x:Name="border" CornerRadius="12" Padding="10 7" Background="#ffffff" BorderBrush="#e0e6ed" BorderThickness="1">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#d9d9d9" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#d9d9d9" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

    </UserControl.Resources>
    <Border Background="#FFBABBBB" CornerRadius="30" >
        <Grid>


            <Grid Grid.Column="1" Margin="30 20 20 20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Separator Height="1" Grid.Row="3" Background="#dae2ea" Margin="0 -1 0 10"/>
                <!--Tab Buttons-->
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0">
                    <Button Name="ShowBtn" Content="Add New Dress" Style="{StaticResource tabButton}" BorderBrush="#FFFFC0C0" Width="748"/>
                </StackPanel>
                <Icon:PackIconMaterial Kind="TshirtV" Width="15" Height="16" VerticalAlignment="Center" Margin="440,20,0,10" Foreground="Black" HorizontalAlignment="Left" Grid.Row="2"/>
                <TextBlock Text="Name:" Grid.Row="4" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0 0 215 0"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="89,8,-59,8" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Name ...." Visibility="{Binding ElementName=Name,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="Name"   Padding="35 15"  Margin="74,0,-74,0"  />

                </Grid>
                <TextBlock Text="Size:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,34,0,262"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" />
                <TextBlock Text="Color:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,84,0,213"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="89,117,-59,-101" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Color ...." Visibility="{Binding ElementName=Color,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="Color"   Padding="35 15"   Margin="74,109,-74,-109"  />

                </Grid>
                <TextBlock Text="Code:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,140,0,156"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="89,173,-59,-157" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Code ...." Visibility="{Binding ElementName=Code,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="Code" PreviewTextInput="NumberValidationTextBox"  Padding="35 15"   Margin="74,165,-74,-165"  />


                </Grid>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="88,222,-58,-206" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Price ...." Visibility="{Binding ElementName=CostPrice,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="CostPrice" PreviewTextInput="NumberValidationTextBox"   Padding="35 15"   Margin="74,215,-74,-215"  />

                </Grid>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" />
                <ComboBox x:Name="SizeSelect"  IsEditable="False" FontSize="13" FontWeight="SemiBold" IsReadOnly="True" HorizontalAlignment="Left" VerticalAlignment="Center"  Text="Select Size"   Width="189" Margin="82,25,0,262"  Height="26" Grid.Row="5"  >
                    <ComboBoxItem Content="50-60"></ComboBoxItem>
                    <ComboBoxItem Content="60-70"></ComboBoxItem>
                    <ComboBoxItem Content="70-80"></ComboBoxItem>
                    <ComboBoxItem Content="80-90"></ComboBoxItem>
                    <ComboBoxItem Content="90-100"></ComboBoxItem>
                    <ComboBoxItem Content="100+"></ComboBoxItem>
                </ComboBox>
                <Button x:Name="AddBtn" Style="{StaticResource addButton}" Click="Button_Click" Margin="0,256,434,22" Grid.Row="5">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Plus"  VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0"/>
                        <TextBlock Text="Add Dress"/>
                    </StackPanel>
                </Button>
                <Button x:Name="Cancel" Style="{StaticResource addButton}" Background="#d32f2f" Click="Cancel_Click"  Margin="0,256,175,22" Grid.Row="5">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Cancel"  VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0"/>
                        <TextBlock Text="Cancel"/>
                    </StackPanel>
                </Button>

                <TextBlock Text="Total Price:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,191,0,105"/>
                <TextBlock Text="Rent Price:" Grid.Row="4" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="440,9,0,5"/>
                <Grid Width="200" HorizontalAlignment="Right" Grid.Row="4" >
                    <TextBlock Margin="-19,8,49,8" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Price ...." Visibility="{Binding ElementName=RentPrice,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="RentPrice" PreviewTextInput="NumberValidationTextBox"  Padding="35 15" Margin="-38,0,38,0"  />


                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
