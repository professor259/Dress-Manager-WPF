<UserControl x:Class="Attendance.View.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Attendance.View"
             mc:Ignorable="d" 
                Background="#eff2f7"
              Style="{StaticResource Page_Style}"
             d:DesignHeight="1024"
              xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" d:DesignWidth="768">
    <Grid Grid.Column="1" Margin="30 20 20 20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Separator Height="1" Grid.Row="3" Background="#dae2ea" Margin="0 0 0 0" />
        <!--Tab Buttons-->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10 0 0">
            <Button IsEnabled="False" Name="ShowBtn" HorizontalAlignment="Center" Content="Settings" Style="{StaticResource tabButton}" BorderBrush="#FFFFC0C0" Width="748"/>

        </StackPanel>
        <TextBlock Text="Launch on Start" Grid.Row="4" FontSize="13" FontWeight="SemiBold" Foreground="#121518"  Margin="20 30 0 0"/>
        <ToggleButton x:Name="StartUp" Grid.Row="4" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20 30 0 0" Click="StartUp_Click" Background="#FFEE7676"/>
    </Grid>
</UserControl>
