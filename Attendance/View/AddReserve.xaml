<UserControl x:Class="Attendance.View.AddReserve"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
               xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Attendance.View"
              xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"  
             mc:Ignorable="d" 
             Background="Transparent"             
           
                 Style="{StaticResource Page_Style}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBoxBase}">
                        <Border x:Name="border" CornerRadius="12" Padding="10 7" Background="#ffffff" BorderBrush="#e0e6ed" BorderThickness="1">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#d9d9d9" TargetName="border"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#d9d9d9" TargetName="border"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

    </UserControl.Resources>
    <Border Background="#FFBABBBB" CornerRadius="30" >
        <Grid>


            <Grid Grid.Column="1" Margin="30 20 20 20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Separator Height="1" Grid.Row="3" Background="#dae2ea" Margin="0 -1 0 10"/>
                <!--Tab Buttons-->
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0">
                    <Button Name="ShowBtn" Content="Add New Reservation" Style="{StaticResource tabButton}" BorderBrush="#784ff2" Width="748"/>
                </StackPanel>
                <Icon:PackIconMaterial Kind="Account" Width="15" Height="16" VerticalAlignment="Center" Margin="451,24,0,15" Foreground="Black" HorizontalAlignment="Left" Grid.Row="2"/>
                <TextBlock Text="Name:" Grid.Row="4" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0 0 215 0"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="89,8,-59,8" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Name ...." Visibility="{Binding ElementName=Name,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="Name"   Padding="35 15"  Margin="74,0,-74,0"  />

                </Grid>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="89,67,-59,-51" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Password ...." Visibility="{Binding ElementName=Password,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>

                </Grid>
                <TextBlock Text="Phone:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,84,0,213"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="89,117,-59,-101" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Phone ...." Visibility="{Binding ElementName=Phone,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="Phone"  PreviewTextInput="NumberValidationTextBox" Padding="35 15"   Margin="74,109,-74,-109"  />

                </Grid>
                <TextBlock Text="Paid:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,140,0,156"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="89,173,-59,-157" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Paid Amount ...." Visibility="{Binding ElementName=Paid,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="Paid" PreviewTextInput="NumberValidationTextBox"   Padding="35 15"   Margin="74,165,-74,-165"  />
                    <TextBox x:Name="Note"   Padding="35 15"   Margin="548,167,-548,-167"  />
                    <TextBlock Margin="563,175,-533,-158" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Note ...." Visibility="{Binding Text.IsEmpty, Converter={StaticResource BoolToVis}, ElementName=Note}"/>

                </Grid>
                <TextBlock Text="Address:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,189,0,107"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" >
                    <TextBlock Margin="89,221,-59,-205" VerticalAlignment="Center" Foreground="#b0b9c6" IsHitTestVisible="False" Panel.ZIndex="1" Text="Enter Address ...." Visibility="{Binding ElementName=Address,Path=Text.IsEmpty,Converter={StaticResource BoolToVis}}"/>
                    <TextBox x:Name="Address"   Padding="35 15"   Margin="74,214,-74,-214"  />

                </Grid>
                <TextBlock Text="Reservation Start:" Grid.Row="4" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="425,10,0,4"/>
                <DatePicker x:Name="StartDate" Loaded="StartDate_Loaded" PreviewMouseLeftButtonDown="StartDate_PreviewMouseLeftButtonDown"  SelectedDate="{x:Static sys:DateTime.Today}" DisplayDateStart="{x:Static sys:DateTime.Today}" Grid.Row="4" SelectedDateChanged="MyDatePicker_SelectedDateChanged" Margin="550,7,0,1"/>
                <TextBlock Text="Reservation End:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="425,84,0,210"/>
                <DatePicker x:Name="EndDate"  SelectedDateFormat="Short" DisplayDateStart="{x:Static sys:DateTime.Today}" Grid.Row="5"  Loaded="EndDate_Loaded"  PreviewMouseLeftButtonDown="EndDate_PreviewMouseLeftButtonDown"  SelectedDateChanged="MyDatePicker_SelectedDateChanged" Margin="550,83,0,205"/>
                <Grid Width="200" HorizontalAlignment="Left" Grid.Row="4" />
                <TextBlock Text="Code:"  FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,31,714,264" Grid.Row="5"/>
                <ComboBox x:Name="CodeSelect"  IsEditable="True" StaysOpenOnEdit="True"  IsTextSearchEnabled="False" FontSize="13" FontWeight="SemiBold" IsReadOnly="False" HorizontalAlignment="Left" VerticalAlignment="Center"  SelectionChanged="FacultySelect_SelectionChanged"  SelectedIndex="-1"   Width="118" Margin="96,22,0,264"  Height="27" Grid.Row="5"/>
                
                
                <Button x:Name="AddBtn" Style="{StaticResource addButton}" Click="Button_Click" Margin="0,256,434,22" Grid.Row="5">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Plus"  VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0"/>
                        <TextBlock Text="Add Reservation"/>
                    </StackPanel>
                </Button>
                <Button x:Name="Cancel" Style="{StaticResource addButton}" Background="#d32f2f" Click="Cancel_Click"  Margin="0,256,175,22" Grid.Row="5">
                    <StackPanel Orientation="Horizontal">
                        <Icon:PackIconMaterial Kind="Cancel"  VerticalAlignment="Center" Width="11" Height="11" Margin="0 1 8 0"/>
                        <TextBlock Text="Cancel"/>
                    </StackPanel>
                </Button>
                <TextBlock Text="Delivery Date:"  Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="425,33,0,261"/>
                <DatePicker x:Name="DeliverDate" SelectedDateFormat="Short" PreviewMouseLeftButtonDown="DeliverDate_PreviewMouseLeftButtonDown" Loaded="DeliverDate_Loaded"  DisplayDateStart="{x:Static sys:DateTime.Today}" Grid.Row="5" SelectedDateChanged="DeliveryDate_SelectedDateChanged"  Margin="550,32,0,256"/>
                <TextBlock x:Name="PriceRent" Text="0 L.E"  FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,31,418,263" Grid.Row="5"/>
                <TextBlock Text="Note:" Grid.Row="5" FontSize="13" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="425,142,0,153"/>

            </Grid>
        </Grid>
    </Border>
</UserControl>
