<Window x:Class="Attendance.WPF.Login"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
         WindowStartupLocation="CenterScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Attendance.WPF"        
        xmlns:li="clr-namespace:Attendance.LoadingSpinnerControl"
        xmlns:wpfanimated="http://wpfanimatedgif.codeplex.com" xmlns:professor="clr-namespace:Attendance.WPF"
        mc:Ignorable="d"
        Title="Login" Height="438.931" Width="311.083">
    <Window.Resources>
        <Storyboard x:Key="WaitStoryboard">
            <DoubleAnimation
        Storyboard.TargetName="Wait"
        Storyboard.TargetProperty="(TextBlock.RenderTransform).(RotateTransform.Angle)"
        From="0"
        To="360"
        Duration="0:0:2"
        RepeatBehavior="Forever" />
        </Storyboard>
    </Window.Resources>
    <Grid Margin="0,0,0,15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="49*"/>
            <ColumnDefinition Width="47*"/>
            <ColumnDefinition Width="207*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="13*"/>
            <RowDefinition Height="151*"/>
            <RowDefinition Height="127*"/>
            <RowDefinition Height="134*"/>
        </Grid.RowDefinitions>
        <Border
            Background="#FF2C2F32"
            CornerRadius="20"  Grid.RowSpan="4" Margin="13,10,15,0" Grid.ColumnSpan="3" >
            <Border.Effect>
                <DropShadowEffect BlurRadius="15"
                              Direction ="-90" 
                              RenderingBias ="Quality" 
                              ShadowDepth ="2" 
                              Color ="Gray" />
            </Border.Effect>

            <StackPanel Orientation="Vertical"
                    Margin="20,44,20,20">
                <TextBlock Name="LoginText" Text="Login" 
                       FontSize="25"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       FontWeight="Bold"
                       Padding="10"
                       />
                <Grid VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Name="UserText" Text="User Name" 
                   Grid.Row="0"
                   Margin="0,15,0,93"
                   HorizontalAlignment="Left" Width="61" Foreground="White"/>
                    <TextBox  Name="UserName"
                  Padding="0,5"
                  Width="243.083"
                  BorderThickness="0,0,0,1"
                  HorizontalAlignment="Left"
                             
                  Text="" Margin="0,37,0,119"/>
                    <TextBlock Name="PassText" Text="Password"
                   Margin="0,103,0,24"
                   HorizontalAlignment="Left" Foreground="White"/>
                    <PasswordBox Name="Password"
                      Password=""
                      Padding="0,5"                           
                      Width="255"
                      BorderThickness="0,0,0,1"
                      HorizontalAlignment="Left" Margin="0,128,0,34" Grid.RowSpan="4" />

                </Grid>
                <Button x:Name="LoginB" 
                    Content="Login" 
                    Foreground="White"
                    Background="Black"
                    Margin="15.042,35,25,35"
                        Click="Login_Click"
                    Height="46">

                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border x:Name="LoginBorder" Background="{TemplateBinding Background}"
                                            CornerRadius="16">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="AliceBlue" TargetName="LoginBorder"  />
                                                <Setter Property="TextBlock.Foreground" Value="Black"  TargetName="LoginBorder" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
        <Grid  Grid.RowSpan="3" Grid.ColumnSpan="3" Margin="46,-14,54,120.645" >

            <Viewbox Stretch="Uniform">
                <Image Source="pack://application:,,,/Images/Acadmic.png"
           SnapsToDevicePixels="True"
           UseLayoutRounding="True"
           RenderOptions.BitmapScalingMode="HighQuality"/>
            </Viewbox>
        </Grid>

        <CheckBox x:Name="ShowPasswordCharsCheckBox" Visibility="Visible" Content="Show password"  Checked="ShowPasswordCharsCheckBox_Checked" Unchecked="ShowPasswordCharsCheckBox_Unchecked" Margin="24,113,158,116.685" Foreground="White" Grid.Row="2" Grid.ColumnSpan="3" Grid.RowSpan="2" />
        <TextBox x:Name="MyTextBox" Text="a7aaaa" Foreground="White"  HorizontalAlignment="Left" Margin="31,75,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.484,0.5" Height="22" Width="255"  Visibility="Hidden" Grid.Row="2" Grid.ColumnSpan="4" />
        <CheckBox Name="AutoLogin" Content="Auto-Login" HorizontalAlignment="Left" Margin="98,117,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.435,0.067" Checked="AutoLogin_Checked" Foreground="White" Grid.Row="2" Height="24" Width="89" Grid.Column="2" Grid.RowSpan="2"/>
        <li:LoadingSpinner x:Name="LoadGif"  IsLoading="True" Cap="Round"  Diameter="4"  Thickness="13" Color="Red"  Grid.ColumnSpan="2"  Margin="0,90.043,47.083,10" Height="160" Grid.Row="2" Grid.RowSpan="2" Grid.Column="1" />
    </Grid>
</Window>
